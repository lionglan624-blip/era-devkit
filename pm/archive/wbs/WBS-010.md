# WBS-010: State Injection拡張 (TFLAG/TEQUIP/TARGET)

## Tasks

| # | Task | Status | Notes |
|---|------|--------|-------|
| 1 | VariableResolver拡張 | [○] | ResolveTflagIndex, ResolveTequipIndex |
| 2 | StateInjector拡張 (TFLAG/TEQUIP) | [○] | Set/GetTflag, Set/GetTequip |
| 3 | 変更点テスト (TFLAG/TEQUIP) | [○] | TFLAG注入成功、TEQUIP遅延注入動作確認 |
| 4 | 回帰テスト (TFLAG/TEQUIP) | [○] | run-all-tests.sh 9/9 PASS |
| 5 | コミット (TFLAG/TEQUIP) | [○] | uEmuera: fdb2c23, Game: 0311491 |
| 6 | StateInjector拡張 (TARGET) | [○] | Set/GetTarget追加 |
| 7 | 変更点テスト (TARGET) | [○] | TARGET注入動作確認 |
| 8 | 回帰テスト | [○] | run-all-tests.sh 9/9 PASS |
| 9 | コミット | [○] | uEmuera: 0cd00b3, Game: e4291f6 |

## Technical Design

### 実装箇所

| ファイル | 変更内容 |
|----------|----------|
| VariableResolver.cs | `ResolveTflagIndex()`, `ResolveTequipIndex()` 追加 |
| StateInjector.cs | `SetTflag()`, `GetTflag()`, `SetTequip()`, `GetTequip()` 追加 |
| StateInjector.cs | `SetTarget()`, `GetTarget()` 追加 ← NEW |
| StateInjector.cs | `InjectVariable()`, `ReadVariable()` のswitch拡張 |

### TFLAG (グローバル配列)

```csharp
// VariableResolver.cs
public static int ResolveTflagIndex(string name)
{
    return ResolveVariableIndex(VariableCode.TFLAG, name);
}

// StateInjector.cs - FLAG と同じパターン
var tflagArray = varData.DataIntegerArray[(int)(VariableCode.__LOWERCASE__ & VariableCode.TFLAG)];
```

### TEQUIP (キャラ紐付け配列)

```csharp
// VariableResolver.cs
public static int ResolveTequipIndex(string name)
{
    return ResolveVariableIndex(VariableCode.TEQUIP, name);
}

// StateInjector.cs - CFLAG と同じパターン
var tequipArray = chara.DataIntegerArray[(int)(VariableCode.__LOWERCASE__ & VariableCode.TEQUIP)];
```

### TARGET (グローバル配列)

```csharp
// StateInjector.cs - FLAG と同じパターン
// 特殊: 値にキャラ名を指定可能 → VariableResolver.ResolveCharacterIndex()で解決
var targetArray = varData.DataIntegerArray[(int)(VariableCode.__LOWERCASE__ & VariableCode.TARGET)];
```

### テストシナリオ

```json
{
  "name": "ufufu-insertion-test",
  "variables": {
    "TARGET:0": "大妖精"
  },
  "characters": {
    "大妖精": {
      "CFLAG:317": 1,
      "TEQUIP:50": 1,
      "TEQUIP:51": 1
    }
  },
  "copy": [
    {"from": "大妖精", "to": "MASTER", "var": "CFLAG:300"}
  ]
}
```

## Acceptance Criteria

- [x] TFLAG注入が動作する (`TFLAG:index=value`)
- [x] TEQUIP注入が動作する (`TEQUIP:character:index=value`)
- [x] TFLAG読み取りが動作する
- [x] TEQUIP読み取りが動作する
- [x] TARGET注入が動作する (`TARGET:index=value` or `TARGET:index=charName`)
- [x] TARGET読み取りが動作する
- [x] 既存のFLAG/CFLAG注入に影響なし (回帰テスト9/9 PASS)

## Log

### 2025-12-11
- WBS作成
- T1: VariableResolver.cs拡張 (ResolveTflagIndex, ResolveTequipIndex追加)
- T2: StateInjector.cs拡張 (SetTflag, GetTflag, SetTequip, GetTequip追加)
- T3: 変更点テスト (TFLAG:102=2注入成功、TEQUIP遅延注入動作確認)
- T4: 回帰テスト 9/9 PASS
- T5: コミット uEmuera: fdb2c23, Game: 0311491
- **TARGET対応追加**: Feature 009テストでTARGET注入が必要と判明
- T6-9: TARGET対応タスク追加
- T6: StateInjector.cs拡張 (SetTarget, GetTarget追加、キャラ名→インデックス解決)
- T6: ScenarioParser.cs拡張 (JsonElement対応、TARGET遅延注入)
- T7: 変更点テスト（TARGET遅延注入動作確認）
- T8: 回帰テスト 9/9 PASS

## Links

- [feature-010.md](feature-010.md)
- [feature-002.md](feature-002.md) - 元のState Injection
- [index-features.md](index-features.md)
